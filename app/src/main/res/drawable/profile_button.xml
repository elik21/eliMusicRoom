<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="40dp"/> <!-- increasing the value, increases the rounding. And as  TTransmit said, to make it like a capsule make the radius half of your button height -->
    <solid android:color="#8647A1"/> <!-- the button color -->
    <gradient
        android:startColor="@color/accent"
        android:endColor="@color/colorAccent"
        android:angle="-270" />
</shape>